Libraries needed to be linked:
------------------------------
-l pthread
-l pcap
-l config "most probably needs to be downloaded and installed firstly before compiling FINS"

Authorization:
---------------
The process runs the pcap has to run with a superuser (root) authorization 
use the command sudo to run it 
